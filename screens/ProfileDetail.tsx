import { SafeAreaView } from "react-native";


export default function ProfileDetail({ route, navigation }: any): JSX.Element {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    )
}